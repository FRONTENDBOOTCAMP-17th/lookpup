# 봐주개(lookpup) 20차 리뷰 — 2026-07-07 (화, 발표 D-1)

발표가 내일입니다. 이번 리뷰는 공개/배포 직전 마지막 안전 점검이라, 그동안 계속 열려 있던 보안 세 항목이 실제로 닫혔는지부터 실측으로 확인했습니다. 결론부터 말하면, **오래 이월되던 [필수·보안] 3종이 이번 회차에 전부 닫혔습니다.** 발표 데모를 자신 있게 열어도 되는 상태라고 봅니다. 아래에 근거를 하나씩 정리해 두었습니다.

---

## 1. 지난 리뷰(19차) [필수·보안] 3종 반영 현황

| 항목 | 19차 | 20차 | 근거(파일:줄 / 실측) |
| --- | --- | --- | --- |
| 1원 결제 취약점 | 미해결 | **해결** | `actions/payments.ts:42` — `createPayment(reservationId, payMethod)`로 시그니처 변경, 클라 `requestedAmount` 인자 자체가 사라짐. 금액은 `reservation.total_price`(:92) 서버값. `verifyAndConfirmPayment:315`가 포트원 재조회 금액과 대조 |
| 공개 RPC 개인정보 노출 | 미해결(실증) | **해결** | anon으로 `POST /rest/v1/rpc/get_petsitters_filtered` → 200이나 `user_id`(인증 UUID) 사라짐, `full_name`→`display_name`, 전체주소→`display_area`(구·동 단위), 좌표 소수점 3자리로 뭉갬 |
| 자기 예약 미차단 | 미해결 | **해결** | `actions/reservations.ts:122-125`(createReservation)·`:987-991`(createPetsitterReservationRequest) 둘 다 `sitterUserId === user.id`면 FORBIDDEN. + 펫 소유권 검증(`:87`,`:928`) |

세 항목 모두 19차 리뷰 직후(7/6 오후) 커밋으로 처리됐습니다. 자기 예약 차단은 영은(0sliverchoi321z)이 `fix: 19차 코드리뷰에 따라 자기 자신에게 예약하는 것을 차단`으로, 1원 결제는 gyuhwa(gyuhwa9922)가 `fix:1원결제 차단`으로 손봤습니다. 리뷰에서 짚은 내용을 커밋 메시지에 그대로 남기며 반영한 흐름이 보기 좋았습니다.

### 공개 전 관점 — 지금 상태로 열어도 됩니다

세 항목이 닫혔으니 데모 중 재현 위험은 사라졌습니다. 다만 발표 전에 딱 두 가지만 눈으로 확인하고 가면 더 안심입니다.

- **결제**: 실제 예약 하나를 만들어 결제까지 태워, 최종 결제창 금액이 서버 예약 금액과 같은지 한 번만 눈으로 대조. (코드상으론 이미 서버값이라 안전하지만, 데모에서 직접 보이면 설득력이 큽니다.)
- **공개 목록**: 로그인 없이 펫시터 목록을 열어, 노출되는 정보가 이름·대략 지역·평점 수준까지인지 확인. (아래 팀 공통에 남은 잔여 노출 한 건이 있는데, 발표 블로커는 아닙니다.)

---

## 2. E2E 결과 (2026-07-07, 포트 3300)

`review/e2e/tests/lookpup-daily.spec.ts`로 사용자 여정(메인 → 펫시터 목록 → 상세 → 로그인 세션 → 예약 → 채팅 → 결제/정산)과 이번에 추가된 흐름(본인인증 페이지, 관리자 라우트)까지 돌렸습니다. 캡처는 `review/images/2026-07-07/`(33장).

| ID | 시나리오 | 결과 |
| --- | --- | --- |
| L1~L12 | 공개 페이지·비인증 접근(메인/펫시터/게시판/채팅/마이프로필/정산/결제/예약/신고) | PASS (200) |
| P1~P3 | 소개·이용약관·개인정보 | PASS (200, /terms 500 해결 유지) |
| A1~A10 | 인증 후 마이프로필·정산·채팅·예약·예약내역·시터편집·알림·설정 | PASS (튕김 없음) |
| A11 | 본인인증 페이지 `/auth/verification` | PASS (200) |
| A12 · A13 | 관리자 `/admin`·`/admin/reports` (비관리자 세션) | **정상 차단** — 200이나 `/`로 리다이렉트 |

- **회귀 0.** 86커밋(delta 최다)이 들어온 회차인데 기존 라우트가 하나도 깨지지 않았습니다.
- **관리자 접근 통제가 제대로 붙었습니다.** `admin/layout.tsx`가 서버 컴포넌트에서 `getUser()` → `users.role` 조회 → `role !== "admin"`이면 `redirect("/")`. 관리자 계정이 아닌 테스트 세션으로 `/admin`을 직접 쳐도 홈으로 튕겨 나옵니다. 클라이언트 숨김이 아니라 서버에서 막는 정석이라, 이번 회차에서 가장 칭찬하고 싶은 부분입니다.
- 카카오 지도는 여전히 빈 캔버스인데, 리뷰 포트가 카카오 콘솔 도메인에 미등록이라 그런 것이고 코드 버그가 아닙니다(계속 이월된 알려진 한계).

정적 검증도 같이 돌렸습니다. **`tsc --noEmit` 0건, `eslint .` 0 error / 23 warning, `npm run build` 성공(59 라우트).** 19차의 eslint 57 error가 0으로 떨어졌습니다(경고만 남음). 빌드도 계속 초록불입니다.

---

## 3. 조원별

### 영은 (0sliverchoi321z) — 채팅·결제·이번 delta 대부분(48커밋)

- **[칭찬] 자기 예약 서버 차단**을 두 진입점(createReservation·createPetsitterReservationRequest) 모두에 붙였습니다. UI에서 `isSelf`로 버튼만 숨기던 걸, 이제 서버 액션이 `sitterUserId === user.id`를 직접 막습니다. URL 직접 호출로 우회하던 구멍이 닫혔습니다.
- **[칭찬] 결제 금액을 서버 진실로 정리.** `createPayment`가 이제 클라가 보낸 금액을 받지 않고 예약 행의 `total_price`만 씁니다. `verifyAndConfirmPayment`는 포트원에 다시 물어본 실제 결제액과 저장액을 대조(`:315`)하고요. "돈은 서버만 안다"는 원칙이 결제 경로에 자리 잡았습니다. 같은 원칙을 채팅 나가기(19차), 자기예약(이번)까지 일관되게 밀고 가는 게 특히 인상적이었습니다.
- **[사소]** `payment/page.tsx:54`의 `displayAmount`(URL 쿼리)는 이제 화면 표시용일 뿐 실제 청구엔 안 쓰이지만, `?amount=1`처럼 URL을 손대면 확인 화면 숫자와 실제 청구액이 어긋나 보일 수 있습니다. 표시값도 `createPayment` 결과(서버 amount)로 맞춰두면 사용자 혼란이 없습니다.
- **[사소]** `payment/page.tsx:98-100` 결제 고객정보가 여전히 `"테스트"/"010-0000-0000"/"test@test.com"` 하드코딩입니다. 실서비스면 로그인 사용자 정보로 채워야 하지만, 발표 데모 범위에서는 급하지 않습니다.

### 박규나 (Gyu-me / qkrrbsk03) — 펫시터·지도·공개 RPC 보안

- **[칭찬] 공개 RPC 개인정보 최소화.** anon으로 목록 RPC를 직접 호출해 봐도 인증 UUID(`user_id`)는 빠졌고, 전체주소는 구·동 단위(`display_area`)로, 집 좌표는 소수점 3자리(약 100m 반경)로 뭉개져 나옵니다. 오래 이월되던 항목이 실측 기준으로 닫혔습니다.
- **[제안]** `display_name`에는 아직 실명으로 보이는 값이 담깁니다(마켓플레이스라 시터 이름 공개는 자연스러운 면이 있지만). 시터 등록 시 활동명/닉네임을 따로 받는지, 아니면 실명이 그대로 나가는 게 의도인지 한 번 짚어두면 좋겠습니다. 의도라면 그대로 두고, 아니라면 표시명 필드를 분리하는 방향입니다.

### gyuhwa (gyuhwa9922) — 결제 로직·정산·관리자

- **[칭찬] 1원 결제 차단.** `createPayment` 시그니처에서 클라 금액 인자를 제거하고 서버 `total_price`로 고정한 게 핵심 처방이었습니다.
- **[칭찬] 관리자 레이아웃 서버 게이트.** `admin/layout.tsx`가 서버에서 role을 확인해 비관리자를 홈으로 돌려보냅니다. 인가를 화면단이 아니라 진입 레이아웃에서 잠근 점이 좋습니다.
- **[사소]** 추가금 결제 실패/취소 시 요청 상태를 pending으로 되돌리는 복구 로직(`cancelPendingPayment:267-271`)까지 챙긴 게 꼼꼼합니다. 상태 전이가 많은 결제 도메인에서 이런 되돌림을 빠뜨리기 쉬운데 잘 잡았습니다.

### BestSeal (sealheal / dngus0074) — 예약 금액 서버계산·게시판(8커밋)

- 19차에서 칭찬한 예약 금액 서버계산(`total_price = service.price × 이용일수`, `reservations.ts:1002`)이 이번 결제 서버검증의 토대가 됐습니다. 결제가 믿는 `total_price`를 서버가 정확히 계산해 두었기에 위쪽 결제 방어선이 성립합니다.
- 이번 회차 담당 영역이 게시판·예약 계산 쪽으로 이어지는데, 발표 자료의 "누가 무엇을 맡았는지" 정리에서 이 부분을 본인 몫으로 분명히 적어두면 좋겠습니다.

---

## 4. 팀 공통

- **[제안] `reviews` 테이블 anon 노출.** 로그인 없이 `GET /rest/v1/reviews`를 치면 `owner_id`·`sitter_id`(둘 다 인증 UUID)와 후기 본문이 그대로 읽힙니다(실측). 후기 자체는 공개가 맞지만, 작성자 UUID까지 나갈 이유는 없습니다. RLS로 노출 컬럼을 좁히거나, 공개용 뷰/RPC로 필요한 필드만 내려주는 방향을 권합니다. 발표 블로커는 아니고, 공개 레포로 올리기 전 정리 대상입니다.
- **[제안] GoTrueClient 이중 인스턴스**는 이번에 정리됐습니다(브라우저 클라이언트가 모듈 싱글턴으로 통합). 지난 회차 [제안]이 닫힌 것으로 확인했습니다.
- **[사소] 정산 훅**(`useEarnings.ts`)은 이제 `res.ok`를 확인하고 실패 시 throw 하도록 되어 있습니다. 에러를 성공으로 삼키던 위험이 없어졌습니다.
- **CI 게이트**: tsc·eslint가 초록불인 지금이 PR 병합 조건으로 `tsc --noEmit && eslint .`를 걸어두기 딱 좋은 시점입니다. 발표 뒤 공개 레포로 이어갈 거라면 이 게이트 하나가 회귀를 크게 줄여줍니다.

---

## 5. 이번 회차 정리

- **[필수·보안] 3종 전부 해결.** 새로 발견한 [필수]는 없습니다.
- 남은 것은 [제안] `reviews` anon UUID 노출, [제안] `display_name` 실명 정책 확인, [사소] 결제 표시금액/더미 고객정보 정도입니다. 전부 발표 이후 정리해도 되는 수준입니다.
- 정적/E2E 모두 초록불(tsc 0, eslint 0 error, build 성공, E2E 33 시나리오 통과, 회귀 0).

오래 붙잡고 있던 세 항목을 발표 하루 전에 마무리 지은 게 이번 회차의 가장 큰 성과라 생각합니다. 내일 발표 잘 갑시다. 화이팅입니다.
